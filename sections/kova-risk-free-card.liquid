{{ 'kova-risk-free-card.css' | asset_url | stylesheet_tag }}

<div class="kova-risk-card-section">
  <div class="page-width">
    <div class="kova-risk-card kova-reveal">
      <div class="kova-risk-icon-floating">
        {% render 'kova-icons', icon: section.settings.main_icon %}
      </div>
      
      <h2 class="kova-risk-heading">{{ section.settings.heading }}</h2>
      <p class="kova-risk-subheading">{{ section.settings.subheading }}</p>

      <div class="kova-risk-badges">
        {% for block in section.blocks %}
          <div class="kova-risk-badge" {{ block.shopify_attributes }}>
            <span class="kova-risk-badge-icon">
              {% render 'kova-icons', icon: block.settings.icon %}
            </span>
            <span>{{ block.settings.title }}</span>
          </div>
        {% endfor %}
      </div>
    </div>
    {% if section.settings.social_proof != blank %}
      <div class="kova-risk-social-proof">{{ section.settings.social_proof }}</div>
    {% endif %}
  </div>
</div>

{% schema %}
{
  "name": "KÖVA Risk Free Card",
  "tag": "section",
  "class": "kova-section",
  "settings": [
    {
      "type": "select",
      "id": "main_icon",
      "label": "Floating Icon",
      "options": [
        { "value": "shield", "label": "Shield" },
        { "value": "check", "label": "Check" },
        { "value": "refresh", "label": "Refresh" },
        { "value": "lock", "label": "Lock" }
      ],
      "default": "shield"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "90-Day Risk-Free Trial"
    },
    {
      "type": "textarea",
      "id": "subheading",
      "label": "Subheading",
      "default": "If it doesn't improve your comfort and focus, return it. No questions asked."
    },
    {
      "type": "text",
      "id": "social_proof",
      "label": "Social Proof Text",
      "default": "Over 10,000+ people have upgraded their work setup with KÖVA."
    }
  ],
  "blocks": [
    {
      "type": "badge",
      "name": "Trust Badge",
      "limit": 3,
      "settings": [
        {
          "type": "select",
          "id": "icon",
          "label": "Icon",
          "options": [
            { "value": "check", "label": "Check" },
            { "value": "shield", "label": "Shield" },
            { "value": "refresh", "label": "Refresh" },
            { "value": "gift", "label": "Gift" }
          ],
          "default": "check"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Text",
          "default": "3-Year Warranty"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "KÖVA Risk Free Card",
      "blocks": [
        { "type": "badge", "settings": { "title": "3-Year Warranty" } },
        { "type": "badge", "settings": { "title": "Free & Easy Returns" } },
        { "type": "badge", "settings": { "title": "Money-Back Guarantee" } }
      ]
    }
  ]
}
{% endschema %}
