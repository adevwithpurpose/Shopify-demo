<div class="rich-text section-{{ section.id }}-padding">
  <div class="page-width">
    <div class="rich-text__wrapper rich-text__wrapper--center">
      <div class="rich-text__blocks center">
        {%- for block in section.blocks -%}
          {%- case block.type -%}
            {%- when 'heading' -%}
              <h2 class="kova-rich-heading kova-reveal {{ block.settings.heading_size }}" {{ block.shopify_attributes }}>
                {{ block.settings.heading_light }}
                <strong>{{ block.settings.heading_bold }}</strong>
              </h2>
            {%- when 'text' -%}
              <div class="kova-rich-text kova-reveal {{ block.settings.text_style }}" {{ block.shopify_attributes }}>
                {{ block.settings.text }}
              </div>
          {%- endcase -%}
        {%- endfor -%}
      </div>
    </div>
  </div>
</div>

<style>
  .kova-rich-heading {
    font-family: var(--font-heading-family) !important;
    font-size: 7.5rem !important;
    font-weight: 300 !important;
    line-height: 1.05;
    color: var(--kova-navy, #122442);
    margin-bottom: 2.5rem;
    letter-spacing: -0.04em;
    text-align: center;
  }
  .kova-rich-heading strong {
    font-weight: 800 !important;
    display: inline;
  }
  .kova-rich-text {
    font-size: 2.2rem;
    color: #555;
    max-width: 850px;
    margin: 0 auto;
    line-height: 1.5;
    font-weight: 400;
    text-align: center;
  }
  @media screen and (max-width: 749px) {
    .kova-rich-heading {
      font-size: 4.5rem !important;
    }
    .kova-rich-text {
      font-size: 1.8rem;
    }
  }
  .kova-rich-text a {
    color: var(--kova-navy);
    font-weight: 700;
    text-decoration: underline;
  }
</style>

{% schema %}
{
  "name": "KÖVA Rich Text",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Top padding",
      "default": 40
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "Bottom padding",
      "default": 40
    }
  ],
  "blocks": [
    {
      "type": "heading",
      "name": "Heading",
      "limit": 1,
      "settings": [
        {
          "type": "text",
          "id": "heading_light",
          "label": "Heading (light)",
          "default": "One Chair ="
        },
        {
          "type": "text",
          "id": "heading_bold",
          "label": "Heading (bold)",
          "default": "Endless Positions"
        }
      ]
    },
    {
      "type": "text",
      "name": "Text",
      "limit": 1,
      "settings": [
        {
          "type": "richtext",
          "id": "text",
          "label": "Text",
          "default": "<p>KÖVA effortlessly adapts to your changing postures, improves comfort and healthy sitting throughout your day</p>"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "KÖVA Rich Text",
      "blocks": [
        { "type": "heading" },
        { "type": "text" }
      ]
    }
  ]
}
{% endschema %}
